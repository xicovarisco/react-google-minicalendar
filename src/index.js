import React, { Component } from 'react';

export default class GoogleMiniCalendar extends Component {
    render() {
        return (
            <div>I am a dummy Calendar component</div>
        )
    }
}